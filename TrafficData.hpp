#pragma once

#include <iostream>
#include <fstream>
#include <map>
#include <string>
#include <sstream>


//I don't remember code for how to get input from a csv file, so I cannot solve this question and have left it blank.
std::map<std::string, int> loadTrafficData(const std::string& filename) {
}

void updateTrafficData(const std::string& filename, std::map<std::string, int>& dataMap) {
}